Flask==2.3.3
Flask-SQLAlchemy==3.0.5
Flask-Login==0.6.3
Flask-SocketIO==5.3.6
Flask-Session==0.5.0
# FIX: Pin Werkzeug — hash behaviour changed between 2.x and 3.x
Werkzeug==2.3.7
python-dotenv==1.0.0
requests==2.31.0
# Self-signed SSL cert generation
cryptography==41.0.7
# PDF text extraction for RAG
pypdf==3.17.4
# Web tools — internet search, page fetching, video transcripts
# FIX: 6.x used curl_cffi with chrome_101 fingerprint which broke on newer curl_cffi.
#      8.x switched to primp (Rust-based) — no fingerprint mismatch possible.
duckduckgo-search==8.1.1
beautifulsoup4==4.12.3
youtube-transcript-api==0.6.2
